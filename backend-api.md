# InkFlow Backend API

本文记录前端可调用的 Tauri IPC 契约。Rust DTO 位于 `src-tauri/src/model.rs`；执行 `pnpm bindings` 会用 `ts-rs` 重建 `src/lib/api/generated/`。生成文件不得手工修改，UI 侧仅可在 `src/lib/api/types.ts` 中收窄已经由后端验证的字符串联合类型。

面向 Agent 的 `inkflow-cli.exe` 不经过 Tauri IPC；其完整命令、JSON 信封、退出码和 Schema 见 [docs/cli.md](docs/cli.md)。桌面适配层和 CLI 适配层调用同一组 Rust 文档、编码、文件、搜索、资源、恢复、设置、会话与导出核心。

所有字段经 IPC 使用 `camelCase`。失败统一序列化为：

```ts
interface ApiError {
  code: string;
  message: string;
}
```

## 共享核心与进程边界

- Cargo 默认 `desktop` feature 构建 Tauri/WebView2，`--no-default-features --features cli` 构建纯 Rust CLI；普通 CLI 命令不会加载 Tauri 或 WebView2。
- 设置、会话与恢复数据使用独立跨进程锁文件。未命名文档的待迁移图片与恢复记录共用恢复锁；桌面端和 CLI 的同一状态写入、图片迁移及清理不会同时提交，恢复版本限频也以锁内读取的磁盘元数据索引为准。文档本身继续使用磁盘修订、条件写入和原子替换；共享同一 `<文档名>.assets` 命名空间的“另存为”目标和已保存资源写入还使用位于系统临时目录的资源目录哈希锁串行化。桌面与 CLI 文档写入、已保存资源写入、渲染/导出结果的最终提交及工作区创建、重命名、移入回收站另有一个工作区外的短时路径变更锁；耗时渲染和 PDF 打印在锁外执行，提交阶段才持锁并复核最初确认的目标仍位于原路径。CLI 与桌面导出在 Windows 上都记录最终目标父目录的文件系统身份，并在提交前以禁止删除共享的目录句柄复核和保持该身份；验证后的同名目录或联接替换会返回 `path_changed`，不能把写入重定向到其他目录。文档事务持锁到最终磁盘修订和后端路径状态更新完成，桌面重命名也在同一锁内同步文档路径，因此不会在原子写入之后移动目标而产生虚假失败或在旧路径重新创建文件。所有这些锁都不会向工作区写文件。必须完成的状态操作在锁竞争时最多等待 5 秒；桌面保存附带的尽力而为检查点与草稿清理使用非阻塞尝试，恢复区正忙或暂时不可写时会跳过并记录警告，不拖慢或中断普通文档保存。显式恢复、设置和会话操作遇到权限、无效路径或锁路径类型错误时会立即返回真实 I/O 错误，不会伪装成 `data_busy`。
- CLI 的片段、HTML 和 PDF 命令才启动配套 `InkFlow.exe` 的隐藏 renderer 模式。该模式不注册单实例和桌面插件，只创建无焦点 renderer 窗口，并使用零通用 capability 权限及带随机令牌的临时请求目录。renderer 请求不包含最终导出路径；HTML/PDF 只生成到同一私有目录并用原子响应声明产物完整，随后由父 CLI 复核目标修订并执行最终原子提交。因此取消或超时可以安全终止任何仍在运行的 renderer，不会等待子进程提交锁，也不会在用户输出目录遗留半成品。父进程在正常完成、取消、超时或子进程失败后统一删除私有目录；在终止边界已经完整落盘的响应仍会被采纳，不会把完成的渲染误报为失败。
- 桌面导出与 CLI renderer 都调用 `src/lib/markdown/export-document.ts`，共享远程图片阻止、本地资源加载、KaTeX、Mermaid strict 和安全净化逻辑；`picture/source/srcset` 与 `img/srcset` 中的本地候选资源也逐项内嵌，加载失败时移除候选并保留可用回退。Mermaid 元数据中的本地 `img` 必须先由同一作用域加载为 `data:` URL；加载失败时保留惰性源码块，绝不把原始本地 URI 交给 Mermaid 直接解析。仅含远程 `srcset` 且没有本地 `src` 的图片会写入统一的远程阻止标记，预览和导出都显示安全占位而不是无响应的空节点。桌面导出开始时用后端令牌冻结文档路径、工作区根、恢复资源根及这些目录的文件系统身份，随后即使标签关闭、文档另存为或工作区切换，资源加载也不会回退到响应式全局状态；目录移动或同名替换会让资源作用域失效并中止导出。
- CLI 公共类型位于 `src-tauri/src/cli/model.rs`，生成的 JSON Schema 位于 `docs/inkflow-cli.schema.json`；它与 `ts-rs` 生成的桌面 IPC 类型是两套独立、受测试约束的契约。

## 文档

### `take_startup_targets`

- 参数：无。
- 返回：`OpenTargetRequest[]`，元素为 `{ kind: "paths", paths: string[] }` 或 `{ kind: "workspace", path: string }`。
- 原子读取并清空首进程启动参数以及前端监听器注册完成前收到的第二实例请求，同时把后续请求切换到事件通道。这样 `app-open-paths` 和 `app-open-workspace` 不会在冷启动监听器空窗期丢失，并保持文件与工作区请求的到达顺序。目录不会混入普通文件列表；工作区由前端调用 `open_workspace`，不会误交给 `open_paths`。

### `open_paths`

- 参数：`{ paths: string[]; updateSettings: boolean }`。用户主动打开文件时传 `true`；会话恢复传 `false`，避免恢复标签时重排最近文件并重复写设置。
- 返回：`DocumentSnapshot[]`。
- 打开与保存使用同一文档操作锁决定规范路径的归属；已登记路径复用原文档 ID，不重置其保存基线。前端同时按 ID 和规范路径去重，保留既有缓冲区，覆盖另存为响应尚未到达时打开目标路径的情况。
- 前端将打开请求直到标签安装完成视为同一次生命周期操作：关闭标签、删除工作区文件和清理冗余 ID 等待此前的打开安装完成，之后的新打开请求等待注销完成。会话恢复使用相同顺序；标签安装后的前端设置写入不占用此等待边界，仍允许保存设置期间关闭并重新打开文件。
- 仅接受 `.md`、`.markdown`、`.mdown`、`.mkd`（扩展名不区分大小写）；其他类型返回 `unsupported_document_type`，不会登记为可编辑文档。文件树中的图片和 PDF 通过 `open_workspace_resource` 交给系统默认程序打开。
- 前端按路径合并进行中的打开请求，并在接收响应时按后端规范路径再次去重；标签安装完成后、任何冗余文档清理或设置写入等待之前释放请求去重标记，避免关闭标签后的重新打开被旧任务吞掉。旧任务收尾仅清理自身仍持有的标记，不能移除后续打开任务的标记；完整操作仍参与窗口关闭等待。已经创建的冗余 ID 通过 `close_document` 释放，不替换现有标签的编辑缓冲区。
- 文件读取、编码识别和哈希计算在 Rust 阻塞任务线程执行，不阻塞窗口消息循环。
- 只接受存在的普通文件；先为整批路径完成读取字节、编码/EOL/BOM 识别和 BLAKE3 修订构造，全部成功后才一次性登记文档。任一路径失败时整批调用不改变后端文档 Store，也不会留下前端不可见的文档。

### `resolve_document_link`

- 参数：`{ documentId: string; href: string }`；返回已校验的规范 Markdown 文件路径。
- 以已登记源文档目录为基准解析相对 URI、百分号编码和父目录路径，解析符号链接后再次验证文件类型。未保存文档返回 `unsaved_document`；绝对路径、外部协议和协议相对地址返回 `unsupported_link`。前端随后通过普通 `open_paths` 打开或激活标签；URI 查询和片段不属于文件名。
- 预览先阻止所有链接的浏览器默认导航：同页片段在预览内滚动，HTTP/HTTPS 和协议相对 URL 交给系统浏览器，其他协议明确拒绝。主 WebView 仅允许首次应用启动导航并拒绝新窗口，避免遗漏的链接或同地址重载丢失内存中的编辑内容。

### `reload_document`

- 参数：`{ documentId: string }`。
- 返回：`DocumentSnapshot`。
- 从磁盘重新读取已打开文档并更新后端修订基线。
- 命令通过 `spawn_blocking` 执行磁盘读取与解码；读盘不持有文档表写锁，提交时校验原路径和修订，拒绝过期重载。
- 前端手动重载、后台重载及冲突比较在接收响应时再次校验文档路径、不可变修订对象和操作代次。保存、重命名和后续重载会使旧响应及旧比较快照失效；保存或重命名进行中不发起重载，避免迟到响应将标签重新绑定到旧文件。
- 保存发生异常后，前端会等待被废弃的在途重载完成，再校验当前路径、修订和操作身份并重新读取磁盘；该同步属于保存队列的一部分，后续保存和窗口关闭会等待它完成。未编辑的标签同步磁盘正文，有新输入时保留输入并标记实际外部冲突；原保存错误和重试入口保留。操作已被替换或标签已关闭时丢弃同步结果，不回退操作代次，也不重新接受旧路径响应。

### `close_document`

- 参数：`{ documentId: string }`。
- 返回：`void`。
- 标签页关闭或工作区文件移入回收站后，移除后端保存与外部变更跟踪状态；恢复记录不会因此删除。关闭标签时前端会先将其移出可编辑界面并清理定时器，再等待既有保存和后端状态清理。删除工作区文件时，从用户确认后到保存、回收站移动和 `close_document` 全部完成前，相关编辑器保持只读，再统一移除标签。两条路径都不会在 IPC 等待窗口接收随后被丢弃的新输入。较早启动、稍后完成的重载会校验原基线，不能把已关闭文档重新登记回来。

### `prepare_save_destination` / `cancel_save_destination`

- 准备参数：`{ documentId: string; path: string }`；返回 `{ token: string; path: string; exists: boolean }`。先解析并验证实际 Markdown 目标，再绑定规范路径、目标是否存在、磁盘修订和父目录身份，同时检查目标是否由其他打开文档占用。
- 前端在保存对话框返回后立即准备目标，早于上传等待和历史图片扫描；目标存在时，覆盖确认对应准备后的快照，包括在原生保存对话框返回前后刚出现的文件。
- 令牌绑定文档 ID 与规范路径，有效期 10 分钟，最多保留 32 个；最终提交消费一次。取消参数为 `{ token: string }`，返回 `void`，重复取消无副作用。前端在取消、成功、失败和放弃保存时释放令牌。

### `save_document` / `save_document_as`

- 普通保存必须对应后端仍登记的文档 ID；已关闭或未知 ID 返回 `document_not_found`，不会创建检查点、重新创建文件或覆盖磁盘内容。未命名文档首次选择路径通过 `save_document_as` 保存；缺少登记不能视为用户确认了覆盖。
窗口关闭另有独立的前端交互锁和共享关闭 Promise，不复用用于放行原生关闭事件的 `closing` 标志。关闭前等待已开始的文档操作、外部变更轮询/重载、保存队列及导出结束，重新检查脏文档并保存最新版本，再持久化会话并销毁窗口；取消或失败会解除交互锁。`reload_document` 会更新后端修订，因此已启动的重载响应仍须按正文/修订竞争检查应用到前端，不能因关闭处理中而丢弃；关闭期间不启动新轮询。

- 普通保存参数：`{ request: SaveDocumentRequest }`；另存为参数：`{ request: SaveDocumentRequest; destinationToken: string }`，其中 `request.path` 必须是准备接口返回的规范路径。
- 返回：`SaveOutcome`。
- 另存为只接受有效的准备令牌，缺失、过期、已消费或绑定不匹配时拒绝提交。准备后目标新建、修改、删除、路径重绑定或父目录替换返回 `save_destination_changed`；不重新采纳提交时的目标修订作为覆盖授权。准备快照在检查点和资源迁移前校验，提交时再次校验，并以相同修订执行条件原子写入；晚期冲突回滚本次新建资源。前端保留原标签路径，提示重新选择，而不把目标冲突登记成源文件的外部变更。
- 保存操作已使旧重载失效时，即使失败原因为目标变化或令牌失效，前端仍等待在途重载并协调源文档：未编辑正文与修订同步到磁盘版本，新输入保留并按需显示源文件冲突，同时保留目标的重新选择提示。
- 规范化目标若被其他文档 ID 占用，在创建检查点、迁移资源和写入之前返回 `document_already_open`，无论目标标签是否有修改。打开请求与另存为串行决定归属；不会静默关闭或替换目标标签。Windows 路径大小写及规范化别名也参与校验。
- 保存目标同样必须使用支持的 Markdown 扩展名；不支持的类型在创建检查点或写入资源前返回 `unsupported_document_type`，避免通过另存为覆盖二进制资源。
- 目标在读取修订和迁移资源前解析为实际路径，并再次校验类型：`.md` 链接指向 `.png`、`.pdf` 等非 Markdown 文件时返回 `unsupported_document_type`；指向 Markdown 文件时，图片目录、写入路径和返回路径均使用实际目标。提交前复核目标及父目录身份。
- 可选 `historyImageSources` 提供撤销、重做分支中去重后的图片 URL（解码 Markdown/HTML 转义，保留 URL 百分号编码）。后端把它们与当前图片一起按源文档作用域解析、迁移，返回可选 `assetRewrites: { source, destination }[]`；生成的 `destination` 保留 URL 编码，前端解码一次后按每个历史状态的实际 Markdown/HTML 上下文重新编码。历史资源不会加入保存正文；仅历史图片发生迁移时 `content` 仍可为 `null`。首次保存也处理历史中的临时资源，复制失败或文档提交冲突时回滚本次新建资源。
- 资源收集和路径改写还包含 `mermaid` 代码围栏元数据根映射的 `img` 与图片型 `icon`。前端渲染、历史扫描和历史重写共用 YAML 事件解析；后端采用相同语法及共享 fixture，按源范围替换标量或别名，保留其他元数据和 Markdown 容器结构，不展开辅助别名图。图片标量定义锚点时，仅在首个非图片别名处重新绑定原值，让后续标签、键和辅助字段保留原有语义；该补充改写与图片改写一同应用于保存正文及撤销/重做历史。流程图 Iconify 标识及 `@` 内置图标不当作文件，时序图的普通 `icon` 值按图片路径处理。普通代码围栏和行内代码不参与迁移，远程、`data:`、`blob:` 资源不复制。
- 首次保存和另存为在历史扫描开始前锁定该标签的正文编辑及图片插入；对话框返回后重新读取标签并等待未完成上传，防止使用旧正文或遗漏在途图片。历史图片由独立 Worker 解析，Worker 不可用时按解析步骤分批执行，历史遍历和重建也会让出主线程。图片路径改写在独立的可编辑状态中重建撤销和重做历史，不受当前编辑器的只读状态或事务过滤器影响，并保持当前编辑锁。切换标签时，编辑器使用创建时的文档 ID 交还状态，由所属标签核对正文并关联版本；后台另存为完成后，缓存中的图片路径、撤销和重做历史同步更新。成功应用返回结果或请求失败后解锁；失败后的重载协调仍允许新输入。
- 图片上传完成和另存为返回后的历史更新按文档串行执行，更新期间暂时锁定所属标签，其他标签仍可编辑。应用结果前重新检查文档是否存在、正文与版本是否一致；基线已变化时重新计算，应用已卸载时丢弃结果。结果合并使用该标签最新的选区、模式及路径，避免异步处理覆盖期间的界面操作。
- 迁移前的 `draft` 检查点使用后端登记的源文档路径，未命名文档保持 `null`；正文中的相对图片仍按源目录恢复，即使后续另存为失败也不会改用目标目录。备份目标文件旧正文的 `history` 检查点仍使用目标路径。
- 普通 `save_document` 的请求路径必须与后端登记路径一致，否则返回 `path_changed`，不写入或重新创建旧路径；省略路径时使用登记路径。已登记文档只有显式 `save_document_as` 才能切换目标或确认覆盖，不能通过普通保存的路径差异跳过修订检查。
- 只读源文档允许另存为其他可写路径；前端拒绝重新选择同一源路径，后端在写入前拒绝只读目标。成功保存后标签清除原只读状态，可以继续编辑和保存副本。
- 引用标签仅按源码标签的大小写与空白归一化匹配，不解码标签里的反斜杠转义或字符引用，避免不同引用定义在并发保存合并时被折叠。
- 后端为 `srcset` 候选独立编码空白和逗号，即使属性带引号也执行；例如另存为 `Copy name.md` 时生成 `Copy%20name.assets/photo.png`。
- 资源复制、临时资源迁移及目标内容比较使用有界读取，单张图片最多 50 MiB，除预检文件大小外最多读取 50 MiB + 1 字节以检测文件增长。超限源图片返回 `resource_too_large`，文档不提交，新建资源回滚；已有的超限同名目标视为占用，选择其他文件名且保留原文件。缺失的待迁移图片返回错误，不把悬空临时引用写入 Markdown。
- 普通保存合并较新正文时，前端同样识别 HTML `img/src`、`img/srcset`、`source/srcset`（含无引号和跨行属性）；同时保留新输入及后端迁移后的路径，再保存最新正文。Markdown 目标范围支持平衡括号、反斜杠转义和标题；图片说明里的内联 HTML、代码及链接按 CommonMark 解析范围跳过，其内部括号不参与图片说明边界判断，替换前复核完整目标。
- 前端按解码 Markdown 转义和 HTML 字符引用后的目标匹配迁移记录，不依赖原始标记语法或引号。生成路径仅做一次 URL 解码，再按当前图片语法重新编码百分号、字符引用定界符、属性引号及 `srcset` 候选分隔符；Markdown 按需保留/补充尖括号。源 URL 的百分号拼写保持区分，以兼容旧版字面 `%xx` 文件名；真实目标变化或迁移映射有歧义时不猜测改写。新增字段可省略，保留既有调用格式。

```ts
type SaveOutcome =
  | { status: "saved"; path: string; revision: DiskRevision; content: string | null; recoveryWarnings: RecoveryWarning[]; assetRewrites?: { source: string; destination: string }[] }
  | { status: "conflict"; path: string; diskRevision: DiskRevision | null }
  | { status: "needsPath" };

type RecoveryWarning = { code: string; message: string };
```

保存前会比较 `expectedRevision`。保存附带的草稿或历史 Recovery 检查点仍为非阻断操作；失败时文档继续保存，并通过 `recoveryWarnings` 返回具体错误，前端按文档与错误码显示五分钟节流的持久警告。Windows 条件保存通过 [`ReplaceFileW`](https://learn.microsoft.com/windows/win32/api/winbase/nf-winbase-replacefilew) 在原子替换发生的同一操作中保留当时的目标版本，再把该备份与 `expectedRevision` 核验；若外部修改恰好发生在最后一次预检查与提交之间，会把被替换版本恢复到原路径并返回冲突，而不是把它静默覆盖。“另存为”会在后端操作开始时记录目标的具体修订或“不存在”状态；即使用户已在文件对话框确认覆盖，随后发生的目标修改或创建仍返回冲突。同一目标的跨进程另存为事务会持有目标哈希锁直到资源复制与文档提交结束；资源复制守卫仅在文档写入成功后提交，失败时只删除本事务创建且内容未被改变的文件。恢复期间若目标又被外部更新，会继续以最新被移走版本为恢复候选；极端持续竞争或系统级回滚失败时保留临时版本路径并返回明确错误。核验成功且已无恢复用途的备份会先改名为带 UUID 的 `inkflow-cleanup` 文件；即时删除受杀毒软件、同步客户端或索引器阻塞时进入单一后台清理队列，每 5 秒重试。原子写入同级文件名只包含 16 位小写十六进制目标文件名哈希、操作类型和 UUID，避免有效的长目标文件名因内部后缀超过 NTFS 255 个 UTF-16 代码单元限制；首次保存、另存为和已有文件替换均使用该有界临时名称。目录清理也必须完整匹配这一格式，不会仅凭 `inkflow-*` 片段删除普通文件。进程崩溃遗留的 `inkflow-write` 文件超过一小时后才具备清理资格；为避免大型目录中的自动保存反复枚举所有同级文件，每个父目录最多每小时统一扫描一次，并在同一次枚举中识别所有目标的 `inkflow-cleanup` 与过期 `inkflow-write` 文件。扫描登记按目录保存，并在超过一小时后惰性淘汰，不会永久积累每个文档路径。已打开的目标文件若被外部删除，也返回 `conflict`，此时 `diskRevision` 为 `null`。后端对保存临界区加锁，前端再按文档串行发送请求，并在响应时核对请求内容，避免旧响应把新输入错误标为已保存。正常写入使用目标目录中的临时文件、刷盘和原子替换，失败最多重试三次；不降级为直接截断覆盖。`content` 仅在未命名资源迁移或“另存为”重写图片相对路径时返回新文本。前端按后端结果中每个实际发生变化的图片位置重建编辑历史，不会因代码块里出现同名示例而清空撤销栈；上传失败时删除占位符也使用同一历史重放机制。“另存为”可复制原文档目录内的 Markdown 图片、引用式图片、HTML `img/src` 以及 `source/srcset` / `img/srcset` 的每个本地候选；写回原始 HTML 时会保留属性的引号上下文，并编码可能破坏属性边界的空白或定界字符。原文档属于当前工作区时，也可复制工作区内的相对图片（包括 `../images` 一类引用）。复制资源不要求原 Markdown 文件仍然存在；若其父目录也已消失，则跳过资源复制但仍保存当前编辑缓冲区。待迁移图片只会在文档写入并登记成功后从恢复区清理。

HTML `srcset` 先解码字符引用，再识别候选图片。前后端维护解码值到原始源码的范围映射，迁移及撤销历史重写保留描述符、引号和分隔符的原始拼写。

### `check_external_changes`

- 同一文档只有路径、修订相同且未被变更操作作废的在途重载才共享 `reload_document` 请求；保存、重命名或接受比较结果会解除共享资格。后续读取等待旧请求结束，再获取新的后端基线，并校验返回快照的路径。重复 Reload、比较或轮询仍可共享同一有效请求，后端的过期修订检查保留。
- 参数：无。
- 返回：`ExternalChange[]`。
- 命令通过 `spawn_blocking` 执行，先在短读锁内复制文档元数据，再在锁外查询磁盘。收集全部观察后，才在一次短写锁内核对整批候选的路径、保存修订及上次观察状态并提交；保存、重载、关闭、路径变更或较新轮询使基线过期时丢弃结果，包括等待后续慢文件期间发生的变更。前端记录发起请求时的路径和不可变修订对象，丢弃 IPC 等待期间已被保存或重载结果替换的文档观察；单纯继续输入不会跳过真正的外部冲突。
- 先比较路径存在性、大小与修改时间；元数据未变化时，每个文档最多每 60 秒做一次完整内容哈希复核，避免短周期轮询反复读取大文件。前端自动重载前会再次确认标签页内容和修订未变化；轮询期间产生的新输入会保留并进入冲突流程。

## 工作区

### `open_workspace`

- 参数：`{ path: string; updateSettings: boolean }`。用户主动打开工作区时传 `true`，记录最近工作区并显示文件树；会话恢复传 `false`，避免改写用户已保存的面板可见性。
- 返回：`WorkspaceSnapshot`。
- 规范化工作区根目录，返回扁平、带 `depth` 的安全文件树。
- 前端串行发送打开请求，并记录后端最后成功选择的工作区。旧响应不会覆盖较新的打开意图；若最新请求失败，界面会对齐到最后成功的工作区，清空旧搜索状态并保留错误提示。恢复出的文件树、最近工作区和会话使用同一根路径；当前根未变化时保留已刷新或修改后的文件树。

### `refresh_workspace`

- 参数：无。
- 返回：`WorkspaceSnapshot | null`。
- 命令通过 `spawn_blocking` 在阻塞任务线程遍历目录；读取根目录后释放工作区读锁，不在扫描期间持有它。前端将创建、重命名、删除、刷新请求及其快照应用放入同一串行队列，失败后继续后续请求；窗口关闭也等待该队列完成。
- 前端创建、重命名、删除和刷新操作返回的快照都校验发起时的工作区根及打开代次；切换工作区（包括 A → B → A）后丢弃旧快照。成功重命名后的标签路径同步、成功删除后的标签关闭仍会完成；旧工作区中新建文件的迟到响应不会自动打开标签。

### `open_workspace_resource`

- 参数：`{ path: string }`。
- 返回：`void`。
- 规范化并校验路径位于当前工作区，只允许普通图片文件（PNG、JPEG、GIF、WebP、SVG、BMP）或 PDF，再由系统默认程序打开；不创建文档标签或自动保存任务。不支持的类型返回 `unsupported_resource`，系统打开失败返回 `open_resource_error`。

### `search_workspace`

- 参数：`{ request: SearchRequest }`。
- 返回：`SearchHit[]`。
- 搜索限制在当前工作区；默认最多 500 条，硬上限 2000。遵循 `.gitignore`，不跟随链接，跳过 `.git`、`node_modules`、`target`、`.idea`、`.vscode` 和超过 20MB 的文件。
- 未打开工作区时搜索请求会被拒绝；不能通过 IPC 临时指定任意扫描根目录。
- 前端切换工作区时清空结果与搜索状态并使在途搜索失效；响应、错误提示和结束状态都同时校验查询代次与工作区根。查询文本保留，用户再次搜索时使用新工作区，旧结果不会重新出现。
- 点击搜索结果时，如果目标标签处于预览模式，先切换为 `live`，等待编辑器挂载后将选区和滚动位置定位到命中行。

### `create_workspace_entry`

- 参数：`{ parent: string; name: string; isDir: boolean }`。
- 返回：更新后的 `WorkspaceSnapshot`。
- 桌面文件树的创建入口使用工作区根目录；创建文件成功后按根目录与新名称组成的完整路径定位条目，不会打开子目录中的同名旧文件。

### `rename_workspace_entry`

- 保留用户输入的目标名称拼写；已存在的目标只有在确认为同一文件系统项且仅名称大小写不同才允许重命名，其他文件、目录或不同名称的硬链接仍返回 `already_exists`。大小写重命名直接使用一次文件系统重命名，成功后同步打开文档的路径；CLI 预演也返回新拼写。
- 实际重命名使用原子“不替换目标”语义（Windows `MoveFileW`，Linux/macOS `renameat_with(NOREPLACE)`），并将提交时的目标冲突转换为 `already_exists`；检查后由外部进程创建的目标仍保留，失败时不更新打开文档的路径。

- 参数：`{ path: string; newName: string }`。
- 返回：更新后的 `WorkspaceSnapshot`。
- 文件或目录重命名成功及失败后，前端都会在恢复自动保存前同步所有仍属于该操作的受影响标签：先等待旧重载完成，再按排队保存结束后的当前路径和修订重新读取。未编辑正文更新为磁盘版本，新输入保留并在必要时显示外部冲突；重命名失败提示保留，不把失败重命名标记成保存失败。较新的操作已接管或标签已关闭时跳过旧同步，旧操作也不能解除新操作的保存暂停。
- 保存暂停由重命名、关闭和删除各自持有独立凭据，操作结束时只释放自己的暂停；释放不依赖文档响应代次。因此已开始的另存为与重命名交错时，无论另存为成功或失败，都不会留下永久暂停；多个操作重叠时，最后一个凭据释放后才恢复自动保存。
- 单个已打开文件直接登记重命名目标路径，只有目录内的子文件才拼接相对后缀；文件路径不会带尾部分隔符，重命名后可继续重载、检测外部修改和保存。

### `trash_workspace_entry`

- 参数：`{ path: string }`。
- 返回：更新后的 `WorkspaceSnapshot`。
- 只允许工作区根目录以内的项目，根目录本身不可删除；操作进入 Windows 回收站。

## 图片资源

- HTML/PDF 导出在当前文档有在途图片上传时拒绝启动并提示等待完成后重试，不创建导出快照或资源租约。完成后重新导出会冻结最终图片地址；其他文档的上传不会阻止当前文档导出。
- Mermaid 渲染器按远程授权选择固定的 iframe 页面；未授权页面通过 CSP `img-src data: blob:` 在解析图表前限制取图，授权切换会销毁旧渲染器。预览、融合和导出输出进入主页面前再解析 CSS，移除外部 URL、资源型 CSS 函数、导入及不明声明，保留本地 SVG 片段引用和普通样式；该策略不依赖 `securityLevel: strict` 或源码正则检测。

- Mermaid 元数据扫描只前进、不回扫失败后缀；每段图表最多 1,048,576 个 UTF-16 单元，元数据/集合嵌套最多 64 层，YAML 别名最多 10,000 个。超出解析预算时在资源加载前保持为惰性源码；未闭合元数据不会重复扫描失败后缀。这些限制不依赖外层异步渲染超时。

### `write_asset`

- 前端插入图片时分别序列化图片描述和资源路径：描述中的 Markdown 标点转义为字面文字，路径保留后端已有的 URL 百分号编码，并处理空格、括号和 Markdown 定界字符。
- 前端独立跟踪每个文档的上传 Promise；保存前和保存对话框返回后等待真实上传任务结束，不用正文中的协议字符串判断是否仍在上传。成功时替换当前及历史状态中的占位符，失败时从所有分支清除对应占位符；当前已撤销插入、标签处于预览或隐藏状态时同样生效，避免重做后永久阻塞保存。

- 参数：`{ request: WriteAssetRequest }`。
- 返回：`WriteAssetResult`。
- 已保存文档写入 `<文档名>.assets`；未命名文档写入恢复区。新资源采用完整内容哈希命名，使 CLI 预演与随后提交得到完全相同的路径；旧版时间戳加哈希文件仍可按完整内容校验并复用。目标已被不同内容占用时返回错误，绝不覆盖。已保存资源写入与同一资源目录的另存为复制/回滚共用跨进程锁，链接成功复用的资源不会被并发失败事务删除。图片最大 50MB，Base64 输入在解码前后都会检查大小；临时资源的文档 ID 和文件名必须是单一路径组件。CLI 的 `--root` 也验证由文档名派生的资源目录，存在符号链接或目录联接时在任何资源写入前拒绝。

### `load_resource`

- 参数：`{ documentId: string; resource: string }`。
- 返回：安全的 `data:` URL。
- 命令为异步入口；恢复区锁等待、资源路径检查、受限文件读取和 Base64 编码整体通过 `spawn_blocking` 执行，不占用原生窗口主线程。资源作用域使用请求开始时捕获的文档路径和工作区根，大小限制与错误码保持不变。
- Markdown 清洗在 `img/src`、`img/srcset` 和 `source/srcset` 中精确保留 `inkflow-asset:` 内部图片引用，预览和导出随后通过文档作用域加载器解析为图片数据。首次保存前及恢复草稿中的图片均走此路径，其他自定义协议、`javascript:`、`file:` 和原始 `data:` 图片引用仍被清洗。
- 只读取恢复区对应文档的临时资源，或文档目录内、最大 50MB 的受支持本地图片；仅当文档自身属于当前工作区时才扩展到工作区范围。渲染器生成的 URL 路径会先执行一次 UTF-8 百分号解码，因此 `<My Note.assets/image.png>` 输出的 `My%20Note.assets/image.png` 仍能映射到真实文件；InkFlow 生成 Markdown 资源链接时会先把文件名中的字面 `%`、`&` 分别编码为 `%25`、`%26`，既保证类似 `100%20done.md` 的合法 Windows 文件名经过一次解码后仍保持字面 `%20`，也避免 `&name;` 被后续 CommonMark/HTML 解析误当成字符引用。对旧版已经生成的未转义字面 `%xx` 资源路径，解码目标不存在时会回退查找原字面路径。解码和兼容候选都要重新校验协议、绝对路径和规范化作用域，拒绝越过允许根目录及远程 URL，包括缺少双斜杠的 `http:` / `https:` 变体。

## 恢复

- 保存成功只清理正文哈希与本次实际提交的原始正文一致的 draft，图片路径迁移前后的文本分别处理；保存等待期间生成的其他版本草稿保留并按常规保留策略清理。未改动正文的保存也使用相同规则。
### `checkpoint_document`

- 参数：`{ request: CheckpointRequest }`。
- 返回：`RecoveryEntry | null`。
- `draft` 最短间隔 2 秒，`history` 最短间隔 60 秒；相同内容不重复写入。记录以 zstd 压缩 JSON 保存。
- 单个正文最大 32 MiB；压缩记录和受限解压 JSON 各自最大 33 MiB，zstd 解码窗口最大 32 MiB，超限返回 `recovery_too_large`。checkpoint 使用带即时字节上限的流式 JSON → zstd 编码；JSON 输出一旦超过 33 MiB 就立即返回 `recovery_too_large`，既不额外保留完整未压缩副本，也不会先处理完转义后大幅膨胀的 JSON。
- Recovery Store 创建时固定根目录的规范路径和文件身份；每次创建 `.recovery.lock`、扫描记录、隔离或删除前先持有并复核该身份。同名目录、符号链接或目录联接替换返回 `path_changed`，不会在替代目标内创建锁、移动记录或执行配额清理。

### `list_recovery`

- 参数：无。
- 返回：按时间倒序的 `RecoveryEntry[]`。
- 清理按每文档 50 个、30 天、有效记录压缩总量 500 MiB 和实际解压 JSON 总量 500 MiB 四个边界执行；本次刚写入的检查点在同一轮清理中优先保留，即使系统时钟回拨使旧记录带有未来时间，也不会报告成功后立即删除新快照。已成功删除的淘汰记录不再占用全局配额，因文件锁或权限问题暂时无法删除的记录仍按实际大小参与后续配额计算。恢复目录维护原子写入的 v2 轻量元数据索引；稳定状态下枚举、限频和清理只读取文件名、压缩/解压大小、修改时间、内容哈希与条目元数据，不再逐个解压历史快照。索引缺失、损坏、不完整或与压缩文件指纹不一致时才用最多读取 33 MiB + 1 字节的 zstd reader 重建，并按实际正文重新验证 size 与哈希。无法读取、超限或校验失败的快照移入 `Quarantine`，最长保留 30 天并计入 500 MiB 磁盘边界；超限时先删除最旧隔离文件，不先淘汰正常历史。`Quarantine` 在枚举或删除前必须验证为 Recovery 规范目录下的真实直属子目录并持有目录身份句柄；符号链接、目录联接、非普通文件及不匹配 InkFlow 隔离命名的条目不会被清理。v2 索引成功原子写入后才移除旧 v1 索引。保存、检查点、枚举、恢复和删除均在 Rust 阻塞任务线程执行，不阻塞窗口和文件关联启动。
- 枚举、恢复、创建和删除都持有恢复目录的跨进程锁；未命名资源写入也持有同一锁。保存只在 Markdown 含有已完成的 `inkflow-asset://` 图片引用时进入迁移事务，并从迁移开始一直持有到文档提交和这些已引用临时文件的清理完成；仍在上传或未被该版本正文引用的文件会保留给后续保存。不同 InkFlow 进程仍按内容哈希全局去重，不只依赖当前进程的内存节流状态。

### `restore_revision`

- 参数：`{ id: string }`。
- 返回：`RestoreOutcome = { document: DocumentSnapshot; warnings: RecoveryWarning[] }`。`document` 使用后端新建的文档 ID，`path`、`revision` 为 `null`；前端直接使用该 ID 建立未保存的可编辑恢复标签，并安排草稿检查点。资源警告包含错误码及错误说明，按图片产生的警告同时包含原引用路径；前端显示持久提示，`warnings` 为空表示未发生资源恢复错误。
- 每次恢复将原 ID 下的待迁移图片复制到新 ID 的恢复目录，同时以快照原路径为基准复制可定位的相对本地图片（原路径属于当前工作区时允许工作区范围），统一改写为新副本的 `inkflow-asset://` 引用。资源使用内容哈希命名，保留原文件，支持同一快照重复恢复及各副本独立保存。缺失、超限、不可访问或复制失败的图片保留原引用并生成警告，继续恢复其他图片；恢复资源目录整体不可用时也返回原正文及警告，不扩大访问范围。快照本身损坏或校验失败仍拒绝恢复。保存时的资源检查保持严格，未解决的临时引用不会被静默写入 Markdown。
- 此返回类型调整仅用于桌面 IPC；CLI `recovery restore` 的 `RecoverySnapshot` 和 `inkflow.cli/v1` JSON 契约保持不变。

### `delete_recovery`

- 参数：`{ id: string }`。
- 返回：`void`。

## 导出

### `prepare_export_source`

- 参数：`{ documentId: string; documentPath: string | null; workspaceRoot: string | null }`。后两项必须来自前端在任何异步等待前创建的不可变导出任务快照。
- 返回：`PreparedExportSource`，即 `{ token: string }`。
- 在打开保存对话框前调用，冻结不可变任务中的文档 ID、规范文档路径、工作区根、恢复资源根和相关目录身份。后端会在捕获资源前后分别核对任务路径与当前文档/工作区状态；若并发“另存为”或工作区切换已经改变作用域，则返回 `invalid_export_source`，不会把旧 Markdown 与新资源目录混合。创建令牌时会在 Recovery 跨进程锁内扫描该文档的待迁移图片并记录每个文件的完整修订；加载 `inkflow-asset://` 时先验证并持有冻结的目录身份，再通过捕获的规范 Recovery 路径获取锁，锁后复核目录和文件修订，因此替代目录不会在拒绝请求前收到 `.recovery.lock`，并发保存造成的迁移、删除或内容变化也返回 `invalid_export_source`，不会被导出管线降级成缺图占位。普通本地资源读取同样在打开的身份句柄保护下复核目录；移动、删除或同名替换也返回 `invalid_export_source`。资源作用域是最多保留 16 个的显式租约，在导出完成、取消、失败或放弃“重新选择”前持续有效，不会因系统保存对话框停留时间而过期；它可在一次导出的多个资源请求及冲突后的重试中复用，只授权资源作用域，不授权最终输出路径。

### `load_export_resource`

- 参数：`{ sourceToken: string; resource: string }`。
- 返回：安全的 `data:` URL。
- 使用令牌捕获的文档/工作区作用域加载恢复区资源或本地图片，不查询当前活动标签和当前工作区。待迁移资源的读取和修订核验在阻塞任务线程及 Recovery 锁内完成；无效、已取消或与准备时修订不一致的令牌/资源会被拒绝。准备作用域时已经不存在的待迁移图片返回普通 `resource_not_found`，由导出管线生成缺图占位；之后新建的同名文件仍不在令牌授权范围内，不会被读取。前端只把令牌失效、目录身份变化或已捕获资源的修订冲突视为整个导出失败，不能把这类作用域授权错误降级为缺图占位后报告成功。

### `cancel_export_source`

- 参数：`{ sourceToken: string }`。
- 返回：`void`。
- 导出完成、取消、失败或用户关闭“重新选择”通知时释放资源作用域；重复取消安全无副作用。

### `prepare_export_destination`

- 参数：`{ path: string }`。
- 返回：`PreparedExportDestination`，即 `{ token: string; path: string }`；`path` 为后端规范化后的绝对最终路径。
- 保存对话框返回后立即调用。后端最多保存 16 个待用令牌，每个有效 10 分钟；快照记录父目录身份以及现有文件修订或“必须不存在”状态。令牌只允许使用一次，并且是最终路径的唯一授权来源。

### `cancel_export_destination`

- 参数：`{ token: string }`。
- 返回：`void`。
- 用户取消、前端渲染失败或任务在提交前结束时释放尚未使用的令牌；重复取消安全无副作用。

### `export_html`

- 参数：`{ request: ExportRequest; destinationToken: string }`，`outputPath` 必填且必须与令牌返回的规范路径完全一致。
- 返回：`ExportOutcome`。
- 写出包含 UTF-8 元数据、打印样式和已内嵌本地图片的单文件 HTML；文档含公式时再内嵌 KaTeX 样式/字体，不依赖相邻 `fonts/` 目录。提交会消费令牌并持有父目录身份句柄执行条件原子写；目标修改、创建、移动、删除或父目录替换返回 `revision_conflict` / `path_changed`，不会静默覆盖。

### `export_pdf`

- 参数：`{ request: ExportRequest; destinationToken: string }`，`outputPath` 必填且与令牌绑定，`pageSize` 为 `A4` 或 `Letter`。
- 返回：`{ action: "saved"; path: string }`。
- Windows 下调用当前 WebView2 的 `PrintToPdf`。前端进入打印布局后最多等待字体 2 秒，避免损坏或不可达字体永久阻塞导出；后端先输出到系统临时目录中的随机、每任务私有目录，该目录通过身份句柄固定并保持到正常完成或迟到回调实际结束，成功后再按令牌快照条件提交，因此渲染期间重绑定目标父目录不会先收到临时文件。WebView2 调用超时为 60 秒；超时后若 WebView2 仍迟到完成，完成回调会清理私有临时文件和目录。若应用异常终止，下一次 PDF 导出会清理超过 24 小时、名称严格匹配 `InkFlow-pdf-<UUID>` 且确认不是符号链接或目录联接的遗留目录；近期目录、相似名称和当前进程仍持有租约的目录不会被触碰。前端每个窗口只允许一个 HTML/PDF 导出任务，并以任务 ID 约束打印 DOM 与清理；冲突通知的“重新选择”会复用原始不可变文档快照创建新令牌。

## 设置

### `get_settings`

- 参数：无。
- 返回：`SettingsV1`。

### `update_settings`

- 参数：`{ settings: SettingsV1; baseline: SettingsV1 }`。`baseline` 必须是调用方生成这些修改所依据的原始设置，不能用提交时的后端当前值代替。
- 返回：规范化后的 `SettingsV1`。
- 页面宽度限制为 560–1400px，字号 12–32px，行高 1.2–2.4，自动保存延迟 250–10000ms；主题只接受 `system`、`light`、`dark`。编辑器与代码字体接受最多 256 个 Unicode 字符的常规 `font-family` 名称列表，并拒绝控制字符、CSS 声明/规则定界符、注释、`url()` 和 `@import`；不安全的磁盘值不会载入或被自动覆盖。
- 后端持有设置文件跨进程锁，并把请求相对显式 `baseline` 改变的字段合并到磁盘最新版本；同一 Store 收到旧快照时也保留其他请求已更新的字段。打开文件、打开工作区和 CLI patch 在锁内读取最新值并只修改所需字段，不提交锁外生成的完整快照。可能等待文件锁的写入在阻塞任务线程执行，不会占用 WebView UI 线程。最近文件与工作区按规范化路径去重并保持上限。
- 前端先把设置变更同步应用到内存，再将布局、最近文件、最近工作区和设置对话框产生的快照放入同一串行写入队列；只有队列中最新操作的规范化响应可以回写内存，避免旧响应或基于旧状态生成的快照覆盖新设置。成功打开文件或工作区后立即持久化对应最近记录。
- 队列的初始基线来自加载结果；已有排队编辑使用上一份成功提交的前端快照作为基线，失败不推进基线。只有最新响应实际应用到界面后，才采用该规范化响应作为下一次编辑的基线，避免将并发后端更新与旧界面值的差异误判为用户修改。
- 首次读取设置失败时使用当前内存值完成 hydration 并继续应用初始化；持久化闸门同时解除，因此后续主题、布局和设置对话框操作仍可正常写入，不需要重启应用。
- 窗口销毁前等待设置写入队列排空，包含等待期间追加的快照。最新写入失败时取消关闭、恢复交互并提示原因；再次关闭会重新提交当前设置后再等待完成。较早的失败已被后续成功快照覆盖时允许关闭，不把队列完成与写入成功混为一谈。

## 会话

### `get_session`

- 参数：无。
- 返回：`SessionV1`。
- 会话只记录工作区路径、已保存文档的标签顺序、每个标签的编辑模式和活动路径，不复制文档正文或未命名草稿。
- 会话文件不存在时返回空会话；文件损坏或无法读取时，首次调用返回 `session_load_warning`，后续调用返回空会话。前端捕获该警告、显示一次汇总提示并继续启动。

### `update_session`

- 参数：`{ session: SessionV1 }`。
- 返回：规范化后的 `SessionV1`。
- 最多保留 50 个非空且不重复的文件路径；模式限制为 `live`、`source`、`preview`，未知模式回退为 `live`，无效活动路径回退到首个标签。
- 会话写入本地应用数据目录的 `session.json`，持有跨进程锁并使用原子写入。CLI 在同一次锁内读取会话正文与对应修订，也可携带期望修订进行条件更新；带 `--root` 的更新和清理只修改作用域内标签并保留不可见工作区状态。桌面前端在标签、活动文档、模式或工作区变化后延迟 350ms 串行写入。瞬时失败最多按 1、2、3 秒退避重试三次，且新状态会取代旧状态的待重试任务。窗口关闭前尝试刷入最新快照；会话写入失败不会阻止用户关闭应用。
- 正常启动单独打开并立即呈现活动文档，再恢复工作区，并以每批最多两个路径恢复其余标签。文件关联或进程启动参数中的显式文件/工作区优先于旧会话；初始化期间由第二实例送达的文件和工作区会按接收顺序排空，缺失文件和不可访问工作区会被跳过而不会阻止启动。

Windows 性能脚本通过进程级 `INKFLOW_PERFORMANCE_PROFILE` 指定一次性应用数据目录，并在发现任何正在运行的 InkFlow 实例时拒绝启动，避免单实例转发或性能会话覆盖用户状态。该变量仅供本地性能验收使用，路径必须为绝对路径。

性能配置启用时，前端在活动文档编辑器完成挂载后调用内部 `mark_performance_ready` 命令，在隔离配置目录写入 `performance-ready` 标记。Windows 性能脚本以该标记和目标文档窗口标题共同判断首屏可交互状态；日常启动不写该文件。

## 路径与权限原则

- WebView capability 只有核心只读窗口能力、显式窗口标题更新、窗口销毁、原生消息/确认及打开/保存对话框和外部 URL 打开权限；窗口销毁与确认权限仅用于完成带未保存检查的关闭流程。
- CLI 隐藏 renderer 使用单独 capability，权限列表为空；文件和导出操作只能通过带随机令牌校验的应用命令进入 Rust 核心。
- 前端不接收通用 `readFile`/`writeFile` 能力。
- 工作区变更在 Rust 侧执行规范化和根目录包含检查。
- 不跟随带 Windows `SYMLINK` 或 `MOUNT_POINT` 重解析标签的工作区符号链接或目录联接；Cloud Files 等其他重解析点按标签区分，不因 `REPARSE_POINT` 属性位本身被拒绝。未知或越界资源返回错误。
- 任何保存冲突、只读、文件锁、磁盘空间或原子替换失败都会保留未保存状态，由恢复记录兜底。
